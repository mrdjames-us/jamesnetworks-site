name: Actions smoke test
on:
  push:
    branches: [claude/workflow-automation-form-builder-erkd3p]
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actions can start jobs on this repo"
